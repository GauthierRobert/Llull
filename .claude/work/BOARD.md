# llull build board — single source of truth for "continue working"

This file is **state**. The `continue-working` skill reads it, reconciles it against the
actual repo, launches parallel agents for the next eligible tasks, and writes it back.
The user only ever has to say **"continue working"**. Humans may edit this file directly.

## Status tokens (line-start, machine-parsed)
- `[TODO]` not started · `[WIP]` an agent is on it now · `[REVIEW]` code done, awaiting `cad-reviewer` + `npm run check`
- `[DONE]` merged & green · `[BLOCKED]` needs a decision/dependency (see note) · `[REMOVED]` deliverable deleted by a later decision (see note)

## Conventions
- **Deps** reference other task IDs. A task is *eligible* only when every dep is `[DONE]`.
- **Lanes** own disjoint file sets (below) so parallel agents don't collide. One task per
  lane runs at a time. Default parallelism = **4 lanes** (+ Lane 5 review runs after each).
- The only shared file is `src/core/commands/registry.ts` — **Lane 1 owns it**; no other
  lane edits it.

## How parallel agents coordinate (they don't talk — they don't need to)
Agents are isolated; coordination is structural, run by the orchestrator:
1. Only the orchestrator writes this board; a task is flipped to `[WIP]` **before** its agent
   launches → the board is the live "who's on what" ledger. Agents never edit it.
2. Disjoint lane file-ownership means two agents **cannot** write the same file.
3. A `[WIP]` lane is busy → never re-assigned while in flight.
4. Cross-lane needs wait on `[DONE]` deps and read the result from **committed files** — the
   repo is the only channel between lanes.
5. `[REVIEW]` (cad-reviewer + `npm run check`, + merge if worktrees) is the backstop.

## Lane → agent → file ownership
| Lane | Agent | Owns (write scope) |
| ---- | ----- | ------------------ |
| 1 Core | `command-author` | `src/core/commands/**`, `registry.ts`, `src/core/model/types.ts` |
| 2 3D+Shell | `viewport-engineer` | `src/ui/viewport/3d/**`, `src/ui/components/**`, `src/ui/panels/**`, `src/ui/store/**` |
| 3 2D | `viewport-engineer` | `src/ui/viewport/2d/**`, snapping/tracking helpers |
| 4 MCP | `mcp-engineer` | `src/core/mcp/**`, `server/**` |
| 5 Review | `test-verifier` + `cad-reviewer` | `tests/**`; reviews every diff (not a blocking lane) |

---

## NOW (auto-updated by the skill)
- **Last updated:** 2026-05-26 — **BATCH 11 DONE + COMMITTED (1291 tests green, 48 files; `7a8c3a1`).** L1 **EN3** spatial `find_entities` (6 filters: nearPoint/insideBBox/overlapsBBox/touchingId/nameFuzzy/tagFuzzy — `query.ts`, 27 tests, 100/96/100/100). L2 **P3** BVH raycasting via three-mesh-bvh (idempotent prototype patch + dispose-paired BVH in all 9 mesh wrappers; 6 tests). L5 nit tests: S2 left-turn fillet + N1 torus volume (R=4, r=0.5) — both pass. `cad-reviewer` APPROVE on full diff. **V4 RE-OPENED** — McpConnect modal was board-claimed but never shipped; only EmptyState exists. — **BATCH 10 RECONCILED + COMMITTED (1256 tests green, 46 files).** Audited off-board WIP from prior sessions w/ `cad-reviewer` → **APPROVE** (no blocking; coverage 96.79/92.95/99.46/96.79). Committed as one batch (`feat: Batch 10`): **P2** InstancedRenderer + grouping helpers (Lane 2) · **U3** 3D adaptive grid + ScaleBar3D HUD (Lane 2) · **VN1** render branches for cone/torus/wedge/pyramid in Entities.tsx (Lane 2) · `make_tube_between` composite command (Lane 1, **60 commands**) · `render.ts` rotation correctness fixed (extrinsic XYZ in tessellators) · **KI5** GitHub Actions `check.yml` + `scripts/check-registry-schema.ts` registry-schema CI guard. Also added `.playwright-mcp/` to `.gitignore`. **A5** (cone/torus/wedge) → superseded by **N1**, marking removed. **AI2** render_view appears to have landed (`server/` vision-loop commits visible — verify next pass). **Launching Batch 11 next:** L1 `EN3` spatial find_entities · L2 `P3` BVH raycasting · L5 follow-up nit tests (S2 left-turn-fillet, N1 torus-volume, V4 focus-restore). — **BATCHES 2–4 DONE (670 tests green, 28 files).** Wave 3 backlog appended (KI/NF/EN). Branch `feat/wave3-batches-2-3` (Batch 2+3 committed `64c48a9`; Batch 4 committing now). Batch 2: `P1`, `AI3` (name/tags + `find_entities`), `V3` (status bar + theme), `U4` (2D adaptive grid + float-origin), `KI2` (data→`structuredContent`). Batch 3: `M1` (7 measure tools), `V2` (palette + shortcuts + view presets), `EN2` (MCP `prompts/`). Batch 4: `Q1` (parameters + expression eval + transitive re-eval), `M2` (measurement HUD + bbox viz), `KI1` (MCP session-keyed docs — concurrent-client isolation). **40 commands.** Every task `cad-reviewer`-gated; reviewers caught + fixed **4 real blocking bugs** (indexed-mesh volume; prompt-plan params/sequencing; Q1 dead-code/cycle-mislabel + a broken test the agent skipped; KI1 session leak). **Batch 5 DONE (719 tests):** L1 `S1` ellipse/spline (+draw cmds; scale/bounds extended exhaustively), L2 `EN8` clipping planes + display modes (shaded/wireframe/xray) + per-entity visibility. **42 commands.** Both `cad-reviewer`-clean (nits applied). Committing. **Batch 6 DONE (789 tests):** L1 `AI4` `check_model` validator · L2 `EN9` named camera views · L3 `VS1` ellipse/spline renderers + draw tools. **43 commands.** All `cad-reviewer`-clean (EN9 blank-name + AI4 cylinder-height nits applied). Committing. **Batch 7 DONE (858 tests):** L1 `L1` layer commands (49 commands) · L2 `EN10` 3D object snapping. Branch pushed to origin. **MCP live-tested:** 49 tools, structuredContent (KI2), prompts (EN2), session isolation (KI1) — all pass. **KI1 session-LEAK found + fixed:** `transport.onclose` doesn't fire on `client.close()` (no DELETE) → added **idle-TTL eviction** (touch-on-request + `.unref()` sweep; env `MCP_SESSION_TTL_MS`/`MCP_SESSION_SWEEP_MS`); empirically proven (abandoned sessions → 404 after TTL). **Batch 8 DONE (958 tests):** L1 `S2` 2D modify commands (offset/fillet/chamfer/trim/extend/explode — geometry review-verified) · L2 `L2` layers panel + undo/redo buttons. **55 commands.** Both `cad-reviewer` APPROVE. Pushed (`25aac35`). **Batch 9 DONE (1052 tests):** L1 `N1` cone/torus/wedge/pyramid solids (**59 commands**) · L2 `V4` onboarding empty-state + MCP-connect affordance · L3 `VS2` 2D-modify pick tools. `N1`/`V4` `cad-reviewer` APPROVE; `VS2` review fast-follow. Committing + pushing. Up next: `VN1` render N1 solids (Lane 2) · `EN1` MCP tool annotations · `EN3` spatial find_entities. Outstanding nit follow-ups: N1 torus-test/mass_properties-desc, V4 focus-tests, S2 left-turn-fillet test. (Pattern noted: agents have over-reported "green" — Q1 broken test, EN10 typecheck, KI1 leak-test — independent verification by the orchestrator caught all three.)
- **Batch 0 (DONE):** made `npm run check` reliably green — `tsconfig.json` `baseUrl` removed + `paths` made relative (forward-compatible with TS 6/7; the global `tsc` 6.0.2 errored on the deprecated `baseUrl`). NOTE: web sessions had no `node_modules` → ensure the SessionStart hook runs `npm install`.
- **Batch 1 (DONE — all 4 lanes merged + pushed, 423 tests green):** L1 `U1` units (`set_units`, units/displayPrecision, `formatLength`; 29 cmds now) · L2 `V1` PBR/IBL/soft-shadows/tone-mapping · L3 `S4` advanced object snaps (perpendicular/tangent/extension/nearest + osnap tracking, +48 tests) · L4 `J1` MCP resources (`cad://document|scene|selection`). `cad-reviewer`: APPROVE on U1/V1/J1 (2 NITs, one applied). S4: math verified correct; CHANGES-REQUESTED fix-pass landed (`46d339e`) — removed dead `osnap-tracking` (deferred as a future feature) + confirmed `nearest`/`extension` already default-off; 420 tests green. **Isolation caveat: J1 + the S4 fix-pass ran in the MAIN checkout (worktree didn't engage) — landed coherently, but watch for it.**
- **Next batches:** B5 = `S1↔VS1` ellipse/spline (unblocks Lane 3) · `L1↔L2` layers · `AI4` check_model (uses W1) · `EN3` spatial find_entities (uses AI3) · `EN1` MCP tool annotations. B6 = `N1↔VN1` cone/torus/wedge/pyramid · `T1↔VT1` text / `T2↔VT2` dimensions · `X1` STL export · `Q2`/`Q3` constraints + feature-history (use Q1). `AI2` render_view deferred pending a headless-GL spike (Windows native-deps risk).
- **Plan of record:** `/root/.claude/plans/prepare-for-next-wave-compiled-tiger.md` — Wave 2 = parametric+measure, modeling depth (2D+3D), UX/visuals/export, MCP/AI intelligence. Kernel swap (K1/K2 fillet/chamfer/shell) DEFERRED by decision.
- **Wave 1 DONE:** W0-1,W0-2, A1,A2,A3,A6, A4-core/A4-ui, R1, B1,B3, C1,C2,C3, D1,D2,D3, E1,E2, G1,G2,G3, H1, I2,I3. **Wave 2 DONE so far:** W1, U2, N0, P1, J2, J3, AI1, **U1, V1, S4, J1 (Batch 1)**.
- **REMOVED (3):** F1,F2,F3 — in-app AI bridge (`core/ai`), `/api/ai` proxy, chat panel. MCP is the sole AI path (decision log).
- **Registered commands (29):** add_box/cylinder/sphere, extrude_profile, move/delete/rotate/scale/mirror_entity, array_linear/polar, draw_line/polyline/arc/circle/rectangle/point, load_document, extrude_sketch, revolve_profile(stub), duplicate/group/ungroup_entities, boolean_union/subtract/intersect, describe_scene, build_project, set_units.

---

## Foundation (gates the UI lanes)
- `[DONE]` **W0-1** Zustand store + `dispatch(name, params)` wrapping `execute()`. _Lane 2. Reviewed clean (APPROVE); select() copies the input array. `@ui/store`: document/lastSummary + dispatch/setDocument/select/toggleSelection/clearSelection; 14 integration tests._
- `[DONE]` **W0-2** Undo/redo via snapshot stack (push on each dispatch). _Lane 2. Reviewed clean (APPROVE). undoStack/redoStack + undo()/redo(); no-ops excluded; setDocument clears history; cap 100; 11 tests; dispatch signature unchanged._

## Lane 1 — Core commands (`command-author`)
- `[DONE]` **A1** Transform commands: `move` / `rotate` / `scale` / `mirror` (`transform.ts`). _deps: —. Added rotate_entity/scale_entity/mirror_entity; reviewed clean; commands/** coverage 100%._
- `[DONE]` **A2** Array commands: linear + polar (`transform.ts`). _deps: A1. Reviewed clean (APPROVE); array_linear + array_polar; +17 tests; commands/** 100%. NIT applied: count schema docs say "integer >= 2"._
- `[DONE]` **A3** Edit commands: `delete` / `duplicate` / `group` / `ungroup` (`edit.ts`). _deps: —. Reviewed (APPROVE); NITs applied (removed dead DEFAULT_LAYER_ID import + void stmt). duplicate_entity + group_entities + ungroup_entities + EntityGroup/groups model (delete_entity untouched); 18 tests; gate held._
- `[DONE]` **A6** (follow-up, from A3 review) `delete_entity` prunes deleted ids from `doc.groups` + dissolves groups <2 members. _Lane 1. deps: A3. Self-verified (orchestrator): 4 tests, commands/** gate held (99.51/92.05/100/99.51)._
- `[DONE]` **A4-core** GeometryKernel port + `mesh` solid kind + boolean_union/subtract/intersect commands. _deps: —. Reviewed (APPROVE); SHOULD-FIX applied (scale-mesh test) + NITs (moved import, dropped dead ?? fallback). kernel.ts + MeshSolidEntity + 3 commands consume operands (+prune groups); tested vs fake kernel; 270 total; gate 99.57/92.71/100/100._
- `[DONE]` **A4-ui** Manifold WASM kernel impl (tessellate operands → CSG → MeshData) injected at startup + `mesh` render branch in 3D viewport. _deps: A4-core. Lane 2. Reviewed (APPROVE); check green (270). manifoldKernel.ts (per-kind tessellation, transforms, `.delete()` cleanup) + setGeometryKernel in main.tsx (non-blocking) + MeshSolidMesh + Entities `case 'mesh'`. **FOLLOW-UPS (need in-browser verify, A4-ui-fix):** (1) rotation uses Manifold extrinsic-XYZ but three.js renders intrinsic-XYZ → multi-axis-rotated operands mis-orient (single-axis OK) — reverse to extrinsic ZYX; (2) `mesh`-operand branch passes a POJO to `ofMesh` (needs `new mod.Mesh(...)`) → stacked boolean-of-boolean silently no-ops; (NIT) align MeshSolidMesh material to BoxMesh._
- `[DONE]` **R1** Remove AI-bridge code (F1/F2/F3). _Done by user edits; agent-verified: core/ai, ChatPanel/useAiChat/chat.css, anthropicClient + /api/ai all deleted; @anthropic-ai/sdk dropped; /mcp + example agent intact; zero dangling refs; root check green (270), server tsc clean._
- `[REMOVED]` **A5** More solids: `cone` / `torus` / `wedge` — **superseded by N1** (which added cone/torus/wedge/pyramid in Batch 9). Kept here for history.
- `[DONE]` **B1** 2D draw commands: line/polyline/arc/circle/rectangle/point (`draw2d.ts` + `Shape2DKind` + `is2D`). _deps: —. Reviewed (APPROVE; 3 minor NITs). 6 commands + Vec2/Shape2DKind/EntityKind/is2D/is3D; scale_entity extended for 2D; 127 tests; commands/** 100/97/100/100. NIT: draw_arc/draw_circle lack the array-shape guard draw_line has on center — robustness follow-up._
- `[TODO]` **B2** `add_text` + `add_dimension` (`annotate.ts`). _deps: B1_
- `[DONE]` **B3** 2D→3D bridge: `extrude_sketch` (+ `revolve_profile` stub) (`profile.ts`). _deps: B1. Reviewed (APPROVE); SHOULD-FIX applied (extrude_profile→extrude_sketch doc ref; removed as-unknown-as cast). extrude_sketch (closed-2D→extrusion, non-destructive) + revolve_profile stub; 16 tests; gate held._
- `[DONE]` **H1** Save/load: serialize/deserialize `CadDocument` (`persistence.ts`). _deps: —. Reviewed (APPROVE); SHOULD-FIX applied (added non-object-root JSON test → 19 tests). serializeDocument/deserializeDocument + load_document; envelope {format,version,document}. NIT (follow-up): import validation is structural only (no finite/range/hex checks)._
- `[TODO]` **H2** Export STL (mesh tessellation). _deps: A5_

## Lane 2 — 3D viewport + UI shell (`viewport-engineer`)
- `[DONE]` **C1** r3f scene + orbit + grid/axes + render branch per solid `kind`. _deps: W0-1. Reviewed (APPROVE); geometry disposal fixed to useEffect; tolerant default added. App shell + Viewport3D + Entities + per-kind meshes._
- `[DONE]` **C2** Selection: raycast, multi-select, highlight → store. _deps: C1. Reviewed clean (APPROVE). onClick raycast → select/toggleSelection; onPointerMissed → clearSelection; highlight from document.selection; single mechanism threaded via Entities onSelect; check green._
- `[DONE]` **C3** Transform gizmo dispatching transform commands. _deps: C2, A1. Reviewed (APPROVE); SHOULD-FIX applied (added transformGizmo.test.ts for the 3 pure delta helpers). drei TransformControls → move/rotate/scale_entity via delta on drag-end; orbit lockout; g/r/s; feedback-loop guarded; undoable._
- `[DONE]` **E1** Toolbar generated from `listCommands()` → dispatch. _deps: W0-1. Reviewed clean (APPROVE); registry-generated buttons → dispatch; mounted in App; 8 tests._
- `[DONE]` **E2** Properties panel + param forms from `paramsSchema`. _deps: W0-1. Reviewed clean (APPROVE). ParamForm (schema→typed inputs→dispatch) + PropertiesPanel (selection + command runner); mounted left dock; 14 tests._
- `[TODO]` **E3** Layers panel (visibility/lock) + undo/redo buttons + shortcuts. _deps: W0-2_

## Lane 3 — 2D viewport (`viewport-engineer`)
- `[DONE]` **D1** Orthographic top-down 2D view + 2D⇄3D toggle + render branch per 2D `kind`. _deps: W0-1, B1. Reviewed (APPROVE); SHOULD-FIX applied (PolylineRenderer memo dep: JSON.stringify→points ref). Viewport2D + Entities2D + 6 renderers; local viewMode toggle; 5 tests._
- `[DONE]` **D2** Snapping (endpoint/midpoint/center/intersection/grid) + ortho/polar tracking. _deps: D1. Reviewed (APPROVE); 2 SHOULD-FIX applied (swept arc midpoint across 0/2π wrap; polarIncrement<=0 → 15° default, +2 tests → 29). pure snapping.ts + useSnap + SnapIndicator._
- `[DONE]` **D3** Interactive draw tools (click-to-place → dispatch B commands). _deps: D2, B1. Reviewed clean (APPROVE; NITs only). useDrawTool + DrawInteraction (snapped clicks) + DrawPreview + DrawTools palette; line/polyline/circle/rectangle/point each via dispatch; Esc cancel; pure drawHelpers + 14 tests._

## Lane 4 — MCP server (`mcp-engineer`)
- `[REMOVED]` **F1** `core/ai` tool-calling bridge. _Removed 2026-05-25 (only MCP for AI control). Deleted `src/core/ai/**` + `tests/unit/ai-bridge.test.ts`._
- `[REMOVED]` **F2** Express `/api/ai` proxy. _Removed 2026-05-25. Deleted the `/api/ai` route + `server/src/anthropicClient.ts`; dropped `@anthropic-ai/sdk`._
- `[REMOVED]` **F3** Chat panel. _Removed 2026-05-25. Deleted `ChatPanel.tsx` + `useAiChat.ts` + `chat.css` + `tests/component/ChatPanel.test.tsx`; unmounted from `App.tsx`._
- `[DONE]` **G1** `core/mcp`: MCP tool defs generated from the registry. _deps: —. Reviewed (APPROVE); buildMcpTools + applyMcpToolCall; unknown-tool detection via getCommand (decoupled from summary text); 14 tests._
- `[DONE]` **G2** Express-hosted MCP endpoint + auth + rate limiting. _deps: G1. Reviewed (APPROVE post-fix); SHOULD-FIX applied: `transport as any` → `transport as Transport` (no any, server tsc clean). /mcp Streamable HTTP → applyMcpToolCall; MCP_AUTH_TOKEN bearer; express-rate-limit._
- `[DONE]` **G3** Example external agent script (end-to-end proof). _deps: G2. Reviewed (APPROVE). server/examples/mcp-agent.ts (MCP client → tools/list + chained add_box/draw_circle/extrude_sketch, id parsed from results); agent:example script; eslint no-console disabled for the CLI demo._

## Lane 5 — Tests & review (`test-verifier`, `cad-reviewer`) — continuous, non-blocking
- `[TODO]` **I1** Hold the `core/commands/**` 90/85/90/90 gate; happy + failure test per new command. _runs with every Lane-1 task_
- `[DONE]` **I2** Integration tests: `store.dispatch` → undo/redo. _deps: W0-2. Satisfied: tests/integration/store.test.ts + undo.test.ts (25 tests)._
- `[DONE]` **I3** Component tests for panels (param-gathering, not geometry). _deps: E2. Satisfied: Toolbar/PropertiesPanel/ViewMode component tests._
- `[TODO]` **I4** `cad-reviewer` pass on every `[REVIEW]` diff before flipping to `[DONE]`.

---

# WAVE 2 — capability expansion (parallelizable backlog)

New work, grouped by theme. Every task carries **Lane**, **deps**, and a one-line acceptance.
`PAIR(x↔y)` = a model/command task (Lane 1) and its viewport render branch (Lane 2/3) that must
land together — schedule them adjacent; the render task deps the command task. Lane file-ownership
is unchanged, so themes interleave freely across the 4 lanes.

## W — Foundations (Lane 1, `command-author`) — gate downstream themes
- `[DONE]` **W1** Extend the command contract for read-only + richer tools: add optional `data?: unknown` to `CommandResult` (query channel; `execute`/MCP pass it through) and add `enum?` + nested-object support to `ParamSpec`. _deps: —. Reviewed APPROVE (`c231eb2`); gate 99.57/92.71/100/99.57. `CommandResult.data` threads through `applyMcpToolCall` (only when present). `ParamSpec` gains `enum`, `type:'object'`+`properties`, recursive `items` via `ParamItemSpec` (optional description → 24 existing commands unchanged). 6 new contract tests. **FOLLOW-UP (M1):** `server/src/mcp.ts` transport drops `result.data` — surface it when the first query command lands._

## N/K — 3D solids & features (Lane 1 cmd + Lane 2 render)
- `[DONE]` **N0** `add_cylinder` + `add_sphere` commands (`geometry.ts`). _deps: —. Merged `a4f72ff` (real worktree). Both mirror `addBox`; graceful no-op on radius/height ≤ 0; registered; 8 tests; commands/** gate 99.6/92.91/100/99.6._
- `[DONE]` **N1** `add_cone` + `add_torus` + `add_wedge` + `add_pyramid`: extend `SolidKind` + entities + commands. _deps: —. PAIR(N1↔VN1). **Batch 9.** Done: 4 entities + 4 commands; exhaustive switches extended in scene.ts (bounds), transform.ts (scale), measure.ts (volume: cone πr²h/3, torus 2π²Rr², wedge box/2, pyramid wbh/3), check.ts (degenerate); +tests. `cad-reviewer` APPROVE-WITH-NITS (exhaustiveness/volumes/bounds independently verified). Gate green (1052). **59 commands.** Follow-up nits: strengthen torus-volume test (tubeRadius≠1 to exercise the square), sync `mass_properties` description to include the 4 new solids._
- `[DONE]` **VN1** Render branches for cone/torus/wedge/pyramid in `Entities.tsx` (three.js `Cone/Torus/...Geometry`, memoized+disposed). _Lane 2. deps: N1. **Batch 10.** Done: per-kind meshes added; tests/component/NewSolidMeshes.test.tsx (16). cad-reviewer APPROVE on full batch diff._
- `[TODO]` **N2** Real `revolve_profile`: closed 2D profile + axis + angle → surface of revolution. Store a parametric `revolution` kind (rendered via `LatheGeometry`, no kernel needed). _deps: B3. PAIR(N2↔VN2)._
- `[TODO]` **VN2** `revolution` render branch (`LatheGeometry`). _Lane 2. deps: N2._
- `[TODO]` **K1** `fillet_edge` + `chamfer_edge` via the `GeometryKernel` interface. _deps: A4-ui. **Likely BLOCKED:** Manifold has no robust edge-fillet/chamfer; may require the OpenCascade.js kernel swap (L9). Surface the decision before building._
- `[TODO]` **K2** `shell_solid` (hollow with wall thickness) via kernel. _deps: A4-ui. Same kernel-capability caveat as K1._
- `[TODO]` **K3** `sweep_profile` (profile along a path) + `loft_profiles` (between sections) → `mesh`. _deps: A4-ui, B3._

## U — Infinite precision & units (the "scroll forever, scale present" ask)
- `[DONE]` **U1** Units system: add `units` (`'mm'|'cm'|'m'|'in'|'ft'`, default `mm`) + display precision to `CadDocument`; `set_units` command; thread through persistence + measure summaries. _Lane 1. deps: —. Acceptance: round-trips through save/load; summaries report values with unit suffix._
- `[DONE]` **U2** Floating-origin / camera-relative rendering: rebase the rendered scene origin to a dynamic offset near the camera target so geometry far from (0,0,0) stays float32-stable (no jitter) — pan/zoom effectively infinite. Document keeps true double coords; offset is render-only (store, not document). _Lane 2. deps: —. Acceptance: a box at 1e7 units renders crisp; orbit/pan stable; selection raycast still correct._
- `[DONE]` **U3** Adaptive infinite grid + on-screen **scale bar / ruler HUD** for the 3D view: grid subdivision steps per zoom decade; HUD shows current unit length (reads `U1` units). _Lane 2. deps: U1, U2. **Batch 10.** Done: `gridHelpers3D.ts` (172 lines) + `ScaleBar3D.tsx` (123 lines); tests/unit/gridHelpers3D.test.ts (27) + tests/component/ScaleBar3D.test.tsx (8). cad-reviewer APPROVE._
- `[DONE]` **U4** 2D view counterpart: adaptive ortho grid + scale bar + infinite pan/zoom in `Viewport2D`. _Lane 3. deps: U1. (U2 technique applied to the ortho camera.) **Batch 2.** Impl done (gridHelpers.ts + ScaleBar + Viewport2D; +27 tests). `cad-reviewer`: **CHANGES-REQUESTED** — B1: floating-origin breaks 2D snapping/click — `DrawInteraction`+`SnapIndicator` capture planes are OUTSIDE the `−renderOrigin` group, so once origin≠0 cursor world-coords and doc-coord snap candidates are in different frames. B2: GridHelper geom-swap has dead allocs + leaky/contradictory code. Nits: StoreInvalidator2D comment wrong ("or zoom"); init refs to initial values. Shared-renderOrigin 2D⇄3D safe for mounted-one-at-a-time (latent split-view thrash — documented constraint). **Fix landed:** SnapIndicator+DrawInteraction moved INTO the −renderOrigin group (frame coherence so cursor e.point == document frame, L7); grid geom-swap cleaned (dead PlaneGeometry/BufferGeometry placeholders + narration removed, no leak); StoreInvalidator2D "or zoom" comment trimmed; refs init to zoom 50 / step 0.5. Orchestrator-verified group at Viewport2D.tsx:348. Batch 2 gate green (478)._

## S — 2D sketch tools (Lane 1 cmd + Lane 3 render/interaction)
- `[DONE]` **S1** New 2D entities `ellipse` + `spline` (Catmull-Rom/Bézier): extend `Shape2DKind` + entities + `draw_ellipse`/`draw_spline` commands. _Lane 1. deps: —. PAIR(S1↔VS1). **Batch 5.** Done: `EllipseEntity{center,radiusX,radiusY}` + `SplineEntity{points,closed}` (Catmull-Rom centripetal, points stored) + `draw_ellipse`/`draw_spline`; `scale_entity` + `scene.ts` `entityBounds` extended (exhaustive over 13 kinds) so measure/find/describe handle them; +tests. `cad-reviewer` APPROVE-WITH-NITS; nits applied (orchestrator): generalized `scale_entity` description to 2D kinds + aligned `draw_polyline` points schema to the nested form. Gate green (719); commands/** 98.48/93.61/100/98.48. **42 commands.** VS1 renderers now unblocked (Lane 3)._
- `[DONE]` **VS1** 2D renderers for ellipse + spline + interactive draw tools. _Lane 3. deps: S1. **Batch 6.** Done: `EllipseRenderer` (EllipseCurve loop) + `SplineRenderer` (CatmullRom centripetal) wired into `Entities2D`; draw tools — 2-click ellipse, multi-click spline (Enter/double-click finish, Esc cancel) — + pure helpers (`ellipseParamsFromCenterCorner`/`validateSplinePoints`) + preview geo + palette buttons; +13 tests. `cad-reviewer` APPROVE (no geometry leak, dispatch-only, demand-mode preview OK). Gate green (789)._
- `[DONE]` **S2** 2D modify commands: `offset_2d`, `fillet_2d`, `chamfer_2d`, `trim`, `extend`, `explode_polyline`. _Lane 1. deps: B1. PAIR(S2↔VS2 for the interactive pick-edge UI). **Batch 8.** Done: 6 commands in `modify2d.ts`; offset sign=left-of-travel (outward for circle/rect); trim/extend move the endpoint nearer the intersection; fillet/chamfer single-corner (`vertexIndex`); explode atomic. +73 tests; modify2d.ts 91.54/89.87/100. Full check green (958). `cad-reviewer` APPROVE-WITH-NITS — **geometry independently re-derived & verified correct** (fillet tangency, offset miters, trim/extend endpoints, NaN guards). Follow-ups (non-blocking): add a left-turn fillet test (the verified cross>0 arc-swap path is untested), use `CircleEntity`/`RectangleEntity` types instead of inline casts, collapse the fillet dead-comment + identical open/closed branches. Note: `trim` uses boundary-SEGMENT bounds (documented, intentional) vs `extend`'s infinite line._
- `[DONE]` **VS2** Interactive 2D modify tools (pick + preview) wired to S2 commands. _Lane 3. deps: S2. **Batch 9.** Done: `useModifyTool` state machine (6 tools → S2 dispatches; Esc cancel / Enter commit) + `ModifyPickInteraction` (in-Canvas nearest-entity pick) + `ModifyTools` palette + pure helpers (`nearestVertex`/`offsetSideSign`/`dist2`); +39 tests (18 helper + 21 component). Gate green (1052). `cad-reviewer` in-flight (committed green; fast-follow any findings)._
- `[TODO]` **S3** `hatch_region` (fill a closed loop with a pattern) + `region` entity. _Lane 1+3. deps: B1._
- `[DONE]` **S4** Advanced object snaps: perpendicular, tangent, extension, nearest + object-snap tracking. _Lane 3. deps: D2. Fix pass committed (`worktree-agent-a58eda50ef1f7a913`, `355934c`): tangent `acos` fix, modes wired through `useSnap`/callers, fake `parallel` dropped. Under `cad-reviewer` (confirm the 3 blocking issues resolved) → then merge._

## T — Annotation (was B2; Lane 1 cmd + render PAIR)
- `[TODO]` **T1** `add_text` (string + height + plane placement). _Lane 1. deps: —. PAIR(T1↔VT1: drei `<Text>`/troika in both 2D & 3D)._
- `[TODO]` **VT1** Text render branch (2D + 3D). _Lane 2+3. deps: T1._
- `[TODO]` **T2** `add_dimension` (linear/aligned/radial/angular) referencing entity ids. _Lane 1. deps: B1, W1 (carry measured value). PAIR(T2↔VT2)._
- `[TODO]` **VT2** Dimension render branch (extension/dimension lines, arrows, text). _Lane 3. deps: T2._

## M — Measurement / query tools (read-only; ideal MCP tools)
- `[DONE]` **M1** Query commands returning `data` (no mutation): `measure_distance`, `measure_angle`, `measure_area`, `measure_perimeter`, `measure_bounding_box`, `measure_volume`, `mass_properties` (density→mass). _Lane 1. deps: W1, U1. Use the `measure` skill. **Batch 3.** Done: 7 read-only commands in `measure.ts`, all return record `data` (KI2-friendly) + `affected:[]` + same doc ref; units via `formatLength` (mm²/mm³); 53→55 tests. `cad-reviewer` CHANGES-REQUESTED→FIXED: `meshVolume` was treating the INDEXED `MeshData` as triangle-soup — rewritten to iterate `indices` (+ unit-cube mesh test that fails on the old formula); nested array schemas tightened. commands/** 98.8/94.29/100/98.8. **38 commands total.** Batch 2+3 gate green (603)._
- `[DONE]` **M2** Measurement HUD overlay: render measure results (distance readout, dimension witness lines, bbox) from the last query. _Lane 2 (3D) + Lane 3 (2D). deps: M1. **Batch 4.** Done: store captures `lastMeasure {command,data}` on any `result.data` (mutating cmds untouched) + `clearLastMeasure`; `MeasurementHUD` (narrow selector, per-shape type guards, dismissable, themed) mounted over both viewports; in-scene bbox viz — 3D `MeasureBBoxWireframe` + 2D `MeasureBBoxRect2D` (memoized+disposed, invalidate on change, inside float-origin group). +23 tests. `cad-reviewer` APPROVE; nits fixed: clear stale measurement on geometry edit (no detached wireframe) + dropped dead lineRef. Gate green (670)._

## L — Layers (Lane 1 cmd + Lane 2 panel)
- `[DONE]` **L1** Layer commands: `add_layer`, `rename_layer`, `set_layer_visibility`, `set_layer_lock`, `set_entity_layer`, `delete_layer` (reassign orphans to default). _Lane 1. deps: —. **Batch 7.** Done: 6 commands in `layers.ts` + optional `Layer.color`; locked-layer policy (set_entity_layer rejects moving OFF a locked source — graceful); delete reassigns orphans to default + refuses default; +30 tests; layers.ts 100/100/100/100. `cad-reviewer` APPROVE-WITH-NITS; lock-scope clause added to description. **49 commands.** Gate green (858). (Broader locked-layer geometry-edit enforcement = follow-up.)_
- `[DONE]` **L2** Layers panel (visibility/lock/active-layer/color) + undo/redo buttons + keyboard shortcuts. (Supersedes old `E3`.) _Lane 2. deps: L1, W0-2. **Batch 8.** Done: `LayersPanel` (row per layer: visibility/lock toggles, inline rename, color swatch read-only, entity count, delete — disabled for default; add-layer form) all via dispatch + `UndoRedoButtons` (store undo/redo + disabled state); mounted right dock. +27 tests. NOTE: `set_layer_color` doesn't exist → swatch read-only (recolor = follow-up). `cad-reviewer` APPROVE (dispatch-only, only-existing-commands confirmed, undo/redo disabled-state correct, strong a11y). Nits (cosmetic): focus-comment accuracy, typed dispatch prop. Gate green (958)._

## Q — Parametric (highest MCP value; Lane 1 model + Lane 2 panels)
- `[DONE]` **Q1** Named parameters/variables in the document + expression evaluation; command params may reference `=width*2`. _Lane 1. deps: W1. Use the `parametric` skill. **Batch 4.** Done: `parameters` on CadDocument + pure recursive-descent `expression.ts` (no eval; +−*/, parens, unary, refs; div-by-zero→Infinity policy) + `set_parameter`/`delete_parameter` with Kahn topo-sort transitive re-eval + cycle detection. `cad-reviewer` CHANGES-REQUESTED→FIXED: removed dead buildEnv/dependsOn, `cycleSet` from topo residue (parse errors no longer mislabeled as cycles), persistence defaults `parameters`. Orchestrator fixed a separate gate-breaker the agent missed (await in non-async test). **40 commands.** Gate green (670); commands/** 98.87/94.09/100/98.87. (Command params accepting `=expr` strings = deliberate follow-up.)_
- `[TODO]` **Q2** Constraints: dimensional (distance/angle driving) + geometric (coincident/parallel/perpendicular/tangent) as first-class document data + a solver pass. _Lane 1. deps: Q1._
- `[TODO]` **Q3** Editable feature history (timeline): promote the undo snapshot stack into a named, replayable command list — insert/reorder/edit-params/suppress → re-evaluate downstream. _Lane 1. deps: W0-2._
- `[TODO]` **VQ** Parameters panel + feature-history timeline panel. _Lane 2. deps: Q1, Q3._

## X — Export / Import (Lane 1 serialization + optional Lane 4 endpoints)
- `[TODO]` **X1** Export STL (ASCII+binary) from tessellated solids/meshes. (Was `H2`.) _Lane 1. deps: N0 (cylinder/sphere tess), A4-ui (mesh). Acceptance: valid STL for box/cylinder/sphere/extrusion/mesh._
- `[TODO]` **X2** Export OBJ + glTF/GLB. _Lane 1/2. deps: X1._
- `[TODO]` **X3** 2D export DXF + SVG from 2D entities. _Lane 1/3. deps: B1._
- `[TODO]` **X4** Import: SVG path → 2D profile, STL → `mesh` entity. _Lane 1. deps: X1/X3._
- `[TODO]` **JX** Optional MCP/server download endpoints for exports. _Lane 4. deps: X1._

## P — Performance (Lane 2/3, `viewport-engineer`)
- `[DONE]` **P1** On-demand rendering: `frameloop="demand"` + `invalidate()` on store/camera changes — idle scenes stop re-rendering (battery/CPU). _Lane 2. deps: —. Committed to main `46e4d24`. `cad-reviewer`: **APPROVE-WITH-NITS** — invalidation coverage complete (selection mutations produce a new `document` ref → StoreInvalidator covers highlight; drei controls self-invalidate); U2 `RenderOriginSyncer` stays live via orbit damping frames. **Carry-forward (→ EN9):** a future programmatic camera/target retarget (fit-to-selection / reset view) MUST call `invalidate()`/`controls.update()` or it could strand the U2 rebase — document the coupling at `Viewport3D.tsx:128` when that lands._
- `[DONE]` **P2** Instanced + merged rendering: `InstancedMesh` for `array_*`/duplicate results and identical primitives; merge static geometry to cut draw calls. _Lane 2. deps: —. **Batch 10.** Done: `InstancedRenderer.tsx` (331 lines) + `grouping.ts` (147 lines) wired into Entities.tsx (batchable kinds grouped per-kind, non-batchable kinds fall back to `EntityRenderer`); geometry/material memoized+disposed (R9); tests/component/InstancedRenderer.test.tsx (14) + tests/unit/grouping.test.ts. cad-reviewer APPROVE._
- `[DONE]` **P3** BVH-accelerated raycasting via `three-mesh-bvh` for selection/snap on large scenes. _Lane 2/3. deps: C2. **Batch 11 (`7a8c3a1`).** Done: `bvhSetup.ts` patches `Mesh.raycast` + `BufferGeometry.{compute,dispose}BoundsTree` (idempotent guard, called at Viewport3D module load → StrictMode safe); all 9 non-batchable mesh wrappers build BVH in `useEffect` and dispose BEFORE `geometry.dispose()` — no leak. InstancedMesh left unchanged (three's per-instance test is already cheap; one-line in-code rationale). tests/unit/bvhSetup.test.ts (6). `cad-reviewer` APPROVE._
- `[TODO]` **P4** LOD + frustum culling + a geometry/material cache & disposal audit (no leaks across re-renders). _Lane 2. deps: —._
- `[TODO]` **P5** (Stretch) WebGPU renderer opt-in behind a flag with WebGL fallback; verify drei helpers (Grid/Gizmo) degrade gracefully. _Lane 2. deps: P1. Re-evaluate only if P1–P4 leave a measured GPU bottleneck._

## V — Design & UX feel (Lane 2/3, `viewport-engineer`)
- `[DONE]` **V1** Material/lighting upgrade: PBR materials, environment map (drei `Environment`), soft contact shadows / AO, tone mapping. _Lane 2. deps: —._
- `[DONE]` **V2** Command palette (Ctrl/Cmd-K over `listCommands()`), global keyboard-shortcut system, and view presets (front/top/right/iso + fit-to-selection). _Lane 2. deps: E1. **Batch 3.** Done: `useKeyboardShortcuts` (Ctrl/Cmd-K, Ctrl-Z/Ctrl-Shift-Z/Ctrl-Y undo/redo — finally wired —, Del→delete_entity, input-suppressed) + `CommandPalette` (fuzzy over listCommands, ↑/↓/Enter, reuses ParamForm) + `ViewPresets` (front/top/right/iso + fit-all/sel; applyPreset calls orbit.update()+invalidate() per P1 carry-forward). `cad-reviewer` APPROVE-WITH-NITS→FIXED: focus-trap+restore implemented (was a real a11y gap), dropped dead `Preset.shortcut`/redundant class, `_innerRef` unmount cleanup; +14 tests (12 palette/9 shortcuts incl. focus-restore). Batch 2+3 gate green (603)._ Done: `useKeyboardShortcuts` (Ctrl/Cmd-K, Ctrl-Z undo, Ctrl-Shift-Z/Ctrl-Y redo, Del/Backspace→delete_entity, suppressed in inputs) + `CommandPalette` (fuzzy over listCommands, ↑/↓/Enter, reuses ParamForm, role=dialog focus-trap, themed) + `ViewPresets` (front/top/right/iso + fit-all/fit-sel; applyPreset calls orbit.update()+invalidate() per P1 carry-forward, comment at Viewport3D RenderOriginSyncer); +19 tests; check green (594)._
- `[DONE]` **V3** Visual design system: refined dark theme + light theme toggle, consistent panel/toolbar styling, iconography, status bar (cursor coords + active units). _Lane 2. deps: U1 (units in status bar). **Batch 2.** Done: `themeStore` (dark/light, localStorage `llull-theme`) + `StatusBar` (units+precision via formatLength, selection count, lastSummary w/ aria-live) mounted in App grid row 3; `styles.css` refactored to CSS variables w/ `[data-theme="light"]`; +13 tests; check green. Cursor coords left as TODO(U4). `cad-reviewer` APPROVE-WITH-NITS; nits applied (orchestrator): theme-adapted 2 stray raw-hex 2D-tool rules, merged imports, dropped redundant role=status. Batch 2 gate green (478)._
- `[WIP]` **V4** First-run empty-state / onboarding hints + MCP "connect an agent" affordance. _Lane 2. deps: —. **Partial — flagged Batch 11.** EmptyState shipped (`tests/component/EmptyState.test.tsx`, 10 tests), but `McpConnect` modal + `McpConnectButton` are MISSING from the repo despite the prior board claim (verified by `find src/ui -iname "*mcp*"` finding only `useMcpLiveDocument.ts`). Lane 5 attempted to add focus-restore test but had no component to test. **Re-open:** Lane 2 needs to build the McpConnect modal (start command + endpoint with Copy, capability badges, focus-trap/Esc/restore) + add a launcher button in `StatusBar`; tests verify focus-trap + focus-restore._

## J — MCP enhancements (Lane 4, `mcp-engineer`)
- `[DONE]` **J1** Expose the document as an MCP **resource** (read-only): list entities, read full document / selection. _deps: G2._
- `[DONE]` **J2** Batch/transaction tool → superseded by `build_project` (see AI theme below). Applies an ordered action list atomically with cross-step aliasing, abort/continue policy, validate dry-run, and a combined report. _Implemented as a registry command (`core/commands/project.ts`), not an MCP-only helper → UI + MCP both gain it._
- `[DONE]` **J3** `describe_scene`: structured read-only snapshot (entity ids, kinds, world bounds, layers, groups, selection) so an agent can orient before editing. _Re-implemented (the stale pre-W1 worktree is gone) as a proper **registry command** in `core/commands/scene.ts` returning the snapshot in `CommandResult.data` (uses W1). 19 tests; document untouched, affected:[]. Cleaner than the old MCP-only built-in: it is a command, so UI + MCP both get it (L1/L5)._

---

## AI — AI-assisted conception (the headline theme; Lane 1 cmd + Lane 4 MCP + Lane 2 UI)
- `[DONE]` **AI1** `build_project`: one tool, a whole project from an AI action plan. Ordered `{command, params, as?}` list run through `execute()`; cross-step `$alias`/`$alias[N]` refs; `onError:'abort'`(full rollback via purity)|`'continue'`; `validate:true` dry-run; per-step report + final scene snapshot in `data`. _Lane 1. `core/commands/project.ts` (+ `scene.ts` reused for the report). 18 tests; commands/** gate held (project.ts 100/93/100/100). Intentional registry↔project circular import is call-time-only (safe)._
- `[TODO]` **AI2** `render_view` (server-side, Lane 4/`server`): headless offscreen render of the current document (front/top/iso) → PNG so a vision agent can SEE its result and self-correct. Lives in `server/` (needs a GL context — forbidden in `core/`); drives the same scene. _deps: G2. Highest-wow AI feedback unlock._
- `[DONE]` **AI3** Named/tagged entities + `find_entities` query: add optional `name`/`tags` to `BaseEntity`; `set_entity_name`/`find_entities` (by kind/layer/name/tag/bbox) returning `data`. Lets a plan say "fillet base_plate" instead of juggling generated ids — makes `build_project` plans far more robust. _Lane 1. deps: W1. PAIR with a small UI rename affordance. **Batch 2.** Impl + `cad-reviewer` (purity/filters/registry confirmed correct). Blocking schema-text mismatch FIXED + nits: descriptions now match code (omit=unchanged, `name:""`/`tags:[]` clear), summary reports final state (`<none>`), +2 omit-preserves tests (478 green). Orchestrator-verified edit.ts:219-273. **Batch 2 gate green (478 tests / 23 files); commands/** gate held.**_
- `[DONE]` **AI4** `check_model` validator (read-only): flags open profiles, degenerate sizes, non-manifold mesh, entities far from origin, empty layers → `data`. The agent's lint pass before/after a `build_project`. _Lane 1. deps: W1. **Batch 6.** Done: `check_model` returns `{ok, issues[]}` (record data); 7 codes: degenerate_size · open_profile · insufficient_points · far_from_origin · empty_layer · orphaned_group_member · parameter_error (surfaces Q1 errors); +30 tests. `cad-reviewer` APPROVE-WITH-NITS; cylinder-height degenerate test added. commands/** 98.8/93.96/100/98.8. **43 commands.**_
- `[TODO]` **AI5** Parametric templates/generators: `instantiate_template` for common parts (gear, flange, bracket, bolt-hole pattern) with params → collapses a long plan into one semantic call. _Lane 1. deps: Q1 (parameters)._
- `[TODO]` **AI6** Persist a `build_project` plan as a named, editable recipe → bridges into feature history (`Q3`): a saved plan IS the regenerable feature tree. _Lane 1. deps: AI1, Q3._

---

# WAVE 3 — "from capabilities to a product"

Wave 1 built the foundation; Wave 2 expands capability (parametric, measure, layers, export, modeling
depth, AI/MCP tools). **Wave 3 hardens that foundation, adds the product-level features that make llull a
*real* CAD app (assemblies, drawings, materials, B-rep), and deepens the highest-leverage existing
surfaces (MCP/AI, parametric, measurement, viewport UX).**

Lane file-ownership and the parallel protocol are **unchanged** (see top of file). Many Wave 3 tasks dep on
Wave 2 deliverables — deps are stated honestly; eligibility is still gated by every dep being `[DONE]`.

**Headline themes (twin pillars):**
1. **"This is real CAD now"** — Assemblies + Drawings on a true B-rep kernel (NF1–NF3, NF5, KI4).
2. **"The best agent-driven CAD"** — deepen the MCP/AI surface: session model, structured data, prompts,
   tool annotations (KI1, KI2, EN1–EN4).

**Sequencing:** front-load the foundation (KI1–KI3, KI5) because the feature work deps on it; then run the
two pillars in parallel across the lanes. Collaboration / simulation / CAM stay "Later" (roadmap) — out of
Wave 3 scope to keep the wave shippable.

## Group 1 — KEY IMPROVEMENTS (foundation & robustness; front-load)
- `[DONE]` **KI1** MCP session & document model: replace the single module-level `workingDoc` in `server/src/mcp.ts` with **session-keyed** documents (SDK stateful transport supplies a session id) + a bridge to load/snapshot the UI document into an MCP session and read it back. _Lane 4. deps: J1. **Batch 4.** Done (server-side isolation half): stateless→stateful refactor — `allocateSession()` holds a per-session `CadDocument` in a closure + `StreamableHTTPServerTransport` w/ `sessionIdGenerator:randomUUID` + `onsessioninitialized/closed` Map; `buildMcpServer(getDoc,setDoc)` takes accessors (no global); POST routes by `mcp-session-id` header (absent→new), GET requires id, DELETE tears down. verify-sessions 7/7 (A's box invisible to B); structured-content 11/11, prompts 31/31, agent:example OK (40 tools). **UI↔MCP live-sync DEFERRED** (TODO marker). `cad-reviewer` CHANGES-REQUESTED — isolation correct but a session/transport LEAK: `onsessionclosed` fires only on HTTP DELETE, and SDK `Client.close()` sends none → every client leaks a session entry (Map grows unbounded). FIXED: chained `transport.onclose`→`sessions.delete` (leak closed for any disconnect, not just DELETE) + close transport on failed init + verify Test 4 (stale id→404 after client close). server typecheck 0; root gate green (670). **UI↔MCP live-sync still deferred (KI1-followup).**_
- `[DONE]` **KI2** Surface `CommandResult.data` over the MCP transport. `core/mcp` already threads `data`; `server/src/mcp.ts` `tools/call` drops it (only forwards summary + affected). Emit it as MCP `structuredContent` (+ a JSON text-block fallback). _Lane 4. deps: W1 (done). Acceptance: a query command's `data` round-trips to an MCP client; resource reads unaffected. (Closes the W1/M1 follow-up.) **Batch 2.** Done: `tools/call` now appends a ```json``` text block + returns `structuredContent` when `result.data!==undefined`; mutating-command path byte-identical. Verified via `server/examples/verify-structured-content.ts` (11/11: describe_scene→full SceneSnapshot structuredContent, add_box→none). server typecheck 0; agent:example clean. `cad-reviewer` APPROVE-WITH-NITS; nits applied (orchestrator): N1 `structuredContent` guarded to non-null non-array objects (future primitive/array `data` surfaces via the JSON text block only — the M1 case); N2 verify-script object check tightened. Batch 2 gate green (478)._
- `[TODO]` **KI3** Constructive vs evaluated geometry split (architecture **L8**): separate the constructive definition (sketches + features + params) from an evaluated cache (`entities`), with a regeneration pass that replays the feature list. The single biggest enabler for parametric + feature history. _Lane 1. deps: Q1, Q3. Acceptance: editing a parameter regenerates dependents; evaluated geometry is reproducible from constructive form; purity + gate held._
- `[TODO]` **KI4** Geometry-kernel decision spike: prototype an `OcctKernel` (OpenCascade.js, B-rep) behind the existing `GeometryKernel` interface (**L9**); measure WASM size / init cost; confirm it unlocks fillet/chamfer/shell (K1/K2) + STEP. _Lane 1/4. deps: A4-ui (done). Acceptance: a written adopt/defer decision + a spike proving one exact boolean and one fillet through the interface, commands untouched. (Resolves the OPEN K1/K2/K3 kernel decision.)_
- `[DONE]` **KI5** CI pipeline: GitHub Actions running `npm run check` + `test:coverage`, server `typecheck`, registry↔`toToolSchemas` 1:1 guard. _Lane 5. deps: —. **Batch 10.** Done: `.github/workflows/check.yml` (uses `npm ci`) + `scripts/check-registry-schema.ts`. cad-reviewer APPROVE._
- `[TODO]` **KI6** Hardened, versioned persistence: H1 import validation is structural only. Add value validation (finite numbers, positive sizes, hex colors, known `kind`s), a `schemaVersion` + forward-migration hook, and a corruption-safe error report. _Lane 1. deps: H1 (done). Acceptance: malformed/old documents load with a clear report or are migrated; round-trip stable; gate held._
- `[TODO]` **KI7** Typed command/tool client bindings (DX): generate typed params/return bindings from the registry so callers (UI param forms, the example agent, tests) get compile-time safety instead of `unknown`. _Lane 1/4. deps: —. Acceptance: a generated typed surface used by ≥1 caller; no `any`; gate held. (Lower priority.)_

## Group 2 — NEW WANTED FEATURES (net-new product capability)
- `[TODO]` **NF1** Assemblies — components & instances: `create_component` (promote a group into a reusable definition), `insert_instance` (placed, transformed reference), `explode_instance`. Add `Component`/`Instance` to the model. _Lane 1. deps: A3 (done). PAIR(NF1↔VNF1). Acceptance: editing a component updates all instances; instances carry their own transform; gate held._
- `[TODO]` **VNF1** Assembly tree panel + instance render branch (instanced where identical). _Lane 2. deps: NF1._
- `[TODO]` **NF2** Mates/joints + BOM: `add_mate` (coincident/parallel/distance between instance frames) solved by the constraint solver; `bill_of_materials` (read-only `data`: component → qty). _Lane 1. deps: NF1, Q2. Acceptance: a mate positions an instance; BOM lists counts; BOM is a read-only query._
- `[TODO]` **NF3** Drawings & sheets (2D documentation from 3D): `create_drawing_view` (orthographic projection of a solid onto a sheet), `add_section_view`, `add_detail_view`; a `sheet`/paper-space concept with title block. _Lane 1 cmd + Lane 3 render. deps: M1; hidden-line ideally KI4 (mesh silhouette acceptable v1). PAIR(NF3↔VNF3). Acceptance: a box yields front/top/right views on a sheet; views update when the model changes._
- `[TODO]` **VNF3** Sheet/paper-space mode: render projected views + title block + sheet-placed dimensions. _Lane 3. deps: NF3._
- `[TODO]` **NF4** Materials library (physical + visual): `create_material` (name, density, PBR color/metalness/roughness), `assign_material`; material drives BOTH `mass_properties` (M) and 3D PBR appearance (V1). _Lane 1 cmd + Lane 2 render. deps: M1, V1 (done). PAIR(NF4↔VNF4). Acceptance: assigning steel vs aluminium changes computed mass and viewport shading._
- `[TODO]` **VNF4** Material picker + per-entity PBR in 3D (replace selection-only highlight with material-driven color/PBR). _Lane 2. deps: NF4._
- `[TODO]` **NF5** STEP / IGES interop (import + export) via the OCC kernel. _Lane 1/4. deps: KI4 (adopt OCC). Acceptance: round-trip a box+fillet through STEP. **BLOCKED on the KI4 decision.**_
- `[TODO]` **NF6** Parametric configurations / design table: `create_configuration` / `activate_configuration` — named parameter-value sets producing model variants from one definition. High MCP value (one tool → a family of parts). _Lane 1. deps: Q1, KI3. PAIR(NF6↔VNF6 configurations panel). Acceptance: switching configuration regenerates the model with the variant's parameter values._
- `[TODO]` **NF7** Direct face editing / push-pull: select a planar face → push/pull to add/remove material. _Lane 1 cmd + Lane 2 interaction. deps: KI4 (face topology). **STRETCH** — needs B-rep faces; defer if OCC deferred._

## Group 3 — ENRICHMENT OF EXISTING FEATURES (deepen Wave 1/2 surfaces)
- `[TODO]` **EN1** MCP tool annotations: add `readOnly`/`destructive` flags to `CommandDefinition` and emit MCP `annotations` (`readOnlyHint`/`destructiveHint`/`idempotentHint`) in `buildMcpTools`/`toToolSchemas` so agents pick safe tools. _Lane 1+4. deps: —. Acceptance: measure/describe advertise `readOnlyHint:true`; `delete_entity` advertises `destructiveHint:true`; existing schemas otherwise unchanged; gate held._
- `[DONE]` **EN2** MCP `prompts/` capability: guided prompt templates ("model a bracket with N bolt holes", "set up an orthographic drawing") that emit a `build_project`-ready plan skeleton. _Lane 4. deps: AI1 (done), J1. **Batch 3.** Done: pure `core/mcp/prompts.ts` (3 templates: `model_bracket`/`orthographic_setup`/`parametric_part`) + thin server handlers (`prompts:{}` + List/GetPrompt) + `verify-prompts.ts`. `cad-reviewer` CHANGES-REQUESTED→FIXED: emitted plans had wrong param names (`draw_rectangle` x/y, `extrude_sketch` position, `boolean_subtract` targetId/toolId) + a subtract-chaining bug — all corrected to the real schemas; **added regression tests running every emitted plan through `build_project` validate against the live registry** (can't silently rot). 44→49 tests. Batch 2+3 gate green (603)._
- `[DONE]` **EN3** Spatial & semantic `find_entities`: extend AI3 with spatial predicates (near point, inside/overlapping bbox, touching) + fuzzy name/tag match; returns `data`. _Lane 1. deps: AI3. **Batch 11 (`7a8c3a1`).** Done in `query.ts` (find_entities was in query.ts, not edit.ts): 6 new optional filters — `nearPoint`, `insideBBox`, `overlapsBBox`, `touchingId`, `nameFuzzy`, `tagFuzzy` — all AND-combined; graceful no-op on each failure; pure (same doc ref, `affected:[]`). tests/unit/findEntitiesSpatial.test.ts (27). `cad-reviewer` APPROVE; nits applied/reverted (one nit revealed ParamSpec doesn't yet support nested `required` — possible future contract enhancement)._
- `[TODO]` **EN4** `render_view` enrichments: multi-angle turntable strip, isolate/highlight an entity id, optional dimension overlay, transparent-section render. _Lane 4/server. deps: AI2. Acceptance: an agent can request an isolated, labeled view of one entity._
- `[TODO]` **EN5** Associative dimensions + dimension styles: T2 dimensions reference live geometry so they update on edit; a dimension style (arrows, text height, precision from U1). _Lane 1 cmd + Lane 3 render. deps: T2. Acceptance: moving a referenced endpoint updates the dimension value._
- `[TODO]` **EN6** 2D polyline/spline editing & richer modify: add/remove/move vertex, convert line↔arc segment, join/close, multi-pick `offset`/`trim`/`extend` (extends S2). _Lane 1 cmd + Lane 3. deps: B1, S2. Acceptance: vertex edits dispatch through commands; gate held._
- `[TODO]` **EN7** Continuous measure mode + measurement report: snap-aware chained measuring with running totals; export a structured measurement report in `data`. _Lane 1 cmd + Lane 2/3 HUD. deps: M1, M2. Acceptance: chained picks accumulate distance; report returns structured `data`._
- `[DONE]` **EN8** Section/clipping planes + display modes in 3D: a clip plane to see inside solids; wireframe / shaded / x-ray modes; per-entity visibility toggle (render-only, in store). _Lane 2. deps: C1 (done). **Batch 5.** Done: render-only `viewportStore` (displayMode shaded/wireframe/xray · clipPlane{enabled,axis,offset,flipped} · hiddenEntityIds Set) + `useMaterialProps` (declarative material overrides, no GPU leak) + `ClippingPlane` (stable Plane ref, gl.localClippingEnabled, no recompile thrash) + `ViewportControls` + per-entity hide in PropertiesPanel + `ViewportStoreInvalidator` (all 3 slices). +33 tests. `cad-reviewer` APPROVE (no doc mutation; nits non-blocking: selected+hidden gizmo floats; global clip cuts grid — documented). Gate green (719)._
- `[DONE]` **EN9** Named views & fit-to-selection: extend V2 presets with savable named camera bookmarks (stored in document camera state) + fit-to-selection. _Lane 2. deps: V2. **Batch 6.** Done: `namedViewStore` (localStorage-persisted save/restore/delete) + in-Canvas bridge (`getSnapshot`/`applyCamera` with `orbit.update()`+`invalidate()` per P1; callbacks nulled on unmount) + overlay UI; +26 tests. `cad-reviewer` APPROVE-WITH-NITS; blank-name save guarded. (fit-to-selection already shipped in V2.) **DEVIATION:** stored in UI store, not the document — promoting to `CadDocument` (persist + MCP-visible) is a Lane-1 follow-up. Gate green (789)._
- `[DONE]` **EN10** 3D object snapping: bring the 2D snap engine (D2/S4) to the 3D viewport for placing/moving solids (vertex/edge/face-center/grid snaps). _Lane 2. deps: D2, S4 (done). **Batch 7.** Done: pure `snap3d.ts` (`collectSnapCandidates3D` per-kind world key points + `snap3d` nearest-within-tol, priority vertex>edge>face-center>grid) + `SnapIndicator3D` (disposed) + gizmo translate-drag snap (change-guarded useFrame, snapped delta → move_entity) + `snap3dEnabled` toggle; +39 tests. `cad-reviewer` APPROVE-WITH-NITS; the `vertices`→`positions` typecheck bug L1 had to patch is confirmed fixed; snap-candidate effect gated on selectedId (was re-running every render). Gate green (858)._
- `[TODO]` **EN11** Layer enrichment: layer color/linetype/lineweight, layer filters/groups, per-drawing-view layer overrides (ties to NF3). _Lane 1 cmd + Lane 2 panel. deps: L1, L2. Acceptance: entity inherits layer color unless overridden; gate held._
- `[TODO]` **EN12** Export fidelity & scope: export-selection-only, batch export, glTF/OBJ with materials (NF4), DXF layer mapping. _Lane 1/2. deps: X1–X3, NF4. Acceptance: exporting a selection with materials produces a valid file carrying material/layer data._

---

## Decision log (resolve `[BLOCKED]` items here)
- **A4 boolean CSG library:** RESOLVED 2026-05-25 → **manifold-3d (Manifold)**. Robust watertight mesh booleans (WASM). Behind a `GeometryKernel` interface (L9) so OpenCascade.js can replace it later for exact B-rep/STEP. Mesh-based results stored as a new `mesh` solid kind.
- **In-app AI bridge:** REMOVED 2026-05-25 (user decision: "only MCP will be used"). Deleted `src/core/ai/**`, the Express `/api/ai` proxy + `anthropicClient.ts`, the chat panel (`ChatPanel`/`useAiChat`/`chat.css`), and the `@anthropic-ai/sdk` dependency. AI control is now delivered solely through the MCP host (`/mcp`). Architecture rules updated: L1 is now "two callers" (UI + MCP). Tasks F1/F2/F3 → `[REMOVED]`.
- **OPEN — kernel capability for K1/K2/K3 (fillet/chamfer/shell):** Manifold (current kernel) does mesh booleans well but has **no robust edge fillet/chamfer or shell**. These need either (a) an OpenCascade.js B-rep kernel swapped in behind the existing `GeometryKernel` interface (L9) — large WASM, slow init, but the "real CAD" path that also unlocks STEP export, or (b) mesh-approximation fillets (lower quality). Decide before scheduling K1/K2. Sweep/loft (K3) are achievable mesh-side. **Default recommendation: defer K1/K2 until an OCC kernel is justified; build N0–N3, sweep/loft, and the rest of Wave 2 first.**
- **OPEN — WebGPU (P5):** stay on WebGL until P1–P4 leave a *measured* GPU bottleneck; the renderer is an isolated `ui/` swap (two `<Canvas>` call sites), so deferring costs nothing.
- **OPEN — OCC kernel adoption (Wave 3 KI4, supersedes the K1/K2/K3 caveat):** the Wave 3 `KI4` spike decides adopt-vs-defer for OpenCascade.js behind the `GeometryKernel` interface (L9). Adopting unlocks fillet/chamfer/shell (K1/K2), STEP/IGES (NF5), and face topology for push-pull (NF7); cost is large WASM + slow init. Decide from the spike before scheduling NF5/NF7/K1/K2.
- **INFO — Wave 3 scope boundary:** real-time collaboration, simulation/CAE, and CAM/fabrication remain "Later" (see `docs/ROADMAP.md`) and are intentionally **out of Wave 3** to keep the wave shippable. Revisit after assemblies + drawings land.
